[package]
name = "hyprs-conf"
version = "0.1.0"
edition = "2024"

[features]
default = ["strict-header", "discovery"]
strict-header = []
discovery = []

[dependencies]
glob = "0.3"
toml = "1"

[dev-dependencies]
tempfile = "3"
